# DocumentLocal

Local storage-specific information (if storageVendor is local)


## Fields

| Field                       | Type                        | Required                    | Description                 |
| --------------------------- | --------------------------- | --------------------------- | --------------------------- |
| `url`                       | *Optional[str]*             | :heavy_minus_sign:          | Local file path             |
| `local_path`                | *Optional[str]*             | :heavy_minus_sign:          | Absolute path on filesystem |