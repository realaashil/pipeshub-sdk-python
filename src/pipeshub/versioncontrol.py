"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from pipeshub import errors, models, utils
from pipeshub._hooks import HookContext
from pipeshub.types import OptionalNullable, UNSET
from pipeshub.utils import get_security_from_env
from pipeshub.utils.unmarshal_json_response import unmarshal_json_response
from typing import Mapping, Optional, Union


class VersionControl(BaseSDK):
    def upload_next_version(
        self,
        *,
        document_id: str,
        file: Union[
            models.UploadNextVersionFile, models.UploadNextVersionFileTypedDict
        ],
        current_version_note: Optional[str] = None,
        next_version_note: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UploadNextVersionResponse:
        r"""Upload next version

        Upload a new version of a versioned document, maintaining full version history.<br><br>
        <b>Overview:</b><br>
        This endpoint creates a new version entry in the document's version history. The previous version remains accessible and the document can be rolled back if needed.<br><br>
        <b>Version Control Flow:</b><br>
        <ol>
        <li>Upload new file content</li>
        <li>System compares with previous version</li>
        <li>If different, creates new version entry</li>
        <li>Updates version history with metadata</li>
        <li>Current document points to new version</li>
        </ol>
        <b>Version Entry Contains:</b><br>
        <ul>
        <li>Version number (auto-incremented)</li>
        <li>Storage URL for this version</li>
        <li>Size and extension</li>
        <li>Optional note describing changes</li>
        <li>User who created version</li>
        <li>Timestamp</li>
        </ul>
        <b>Requirements:</b><br>
        <ul>
        <li>Document must have <code>isVersionedFile: true</code></li>
        <li>File extension must match original document</li>
        <li>Content must differ from previous version</li>
        </ul>
        <b>File Constraints:</b><br>
        <ul>
        <li>Maximum size: 100MB</li>
        <li>Same extension as original required</li>
        </ul>


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param file: New version file (max 100MB)
        :param current_version_note: Note describing the current version being replaced
        :param next_version_note: Note describing the new version being uploaded
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UploadNextVersionRequest(
            document_id=document_id,
            body=models.UploadNextVersionRequestBody(
                current_version_note=current_version_note,
                next_version_note=next_version_note,
                file=utils.get_pydantic_model(file, models.UploadNextVersionFile),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/document/{documentId}/uploadNextVersion",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "multipart",
                models.UploadNextVersionRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadNextVersion",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UploadNextVersionResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "403", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def upload_next_version_async(
        self,
        *,
        document_id: str,
        file: Union[
            models.UploadNextVersionFile, models.UploadNextVersionFileTypedDict
        ],
        current_version_note: Optional[str] = None,
        next_version_note: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UploadNextVersionResponse:
        r"""Upload next version

        Upload a new version of a versioned document, maintaining full version history.<br><br>
        <b>Overview:</b><br>
        This endpoint creates a new version entry in the document's version history. The previous version remains accessible and the document can be rolled back if needed.<br><br>
        <b>Version Control Flow:</b><br>
        <ol>
        <li>Upload new file content</li>
        <li>System compares with previous version</li>
        <li>If different, creates new version entry</li>
        <li>Updates version history with metadata</li>
        <li>Current document points to new version</li>
        </ol>
        <b>Version Entry Contains:</b><br>
        <ul>
        <li>Version number (auto-incremented)</li>
        <li>Storage URL for this version</li>
        <li>Size and extension</li>
        <li>Optional note describing changes</li>
        <li>User who created version</li>
        <li>Timestamp</li>
        </ul>
        <b>Requirements:</b><br>
        <ul>
        <li>Document must have <code>isVersionedFile: true</code></li>
        <li>File extension must match original document</li>
        <li>Content must differ from previous version</li>
        </ul>
        <b>File Constraints:</b><br>
        <ul>
        <li>Maximum size: 100MB</li>
        <li>Same extension as original required</li>
        </ul>


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param file: New version file (max 100MB)
        :param current_version_note: Note describing the current version being replaced
        :param next_version_note: Note describing the new version being uploaded
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UploadNextVersionRequest(
            document_id=document_id,
            body=models.UploadNextVersionRequestBody(
                current_version_note=current_version_note,
                next_version_note=next_version_note,
                file=utils.get_pydantic_model(file, models.UploadNextVersionFile),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/document/{documentId}/uploadNextVersion",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "multipart",
                models.UploadNextVersionRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadNextVersion",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UploadNextVersionResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "403", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def rollback(
        self,
        *,
        document_id: str,
        version: str,
        note: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.RollBackToPreviousVersionResponse:
        r"""Rollback to previous version

        Restore a versioned document to a previous version. Creates a new version entry with the rolled-back content.<br><br>
        <b>Overview:</b><br>
        This endpoint allows reverting a document to any previous version while maintaining the complete version history. The rollback itself creates a new version entry.<br><br>
        <b>Rollback Flow:</b><br>
        <ol>
        <li>Specify target version number to rollback to</li>
        <li>System retrieves content from that version</li>
        <li>Creates new version entry with restored content</li>
        <li>Adds rollback note to version history</li>
        <li>Document now shows restored content</li>
        </ol>
        <b>Version History Preserved:</b><br>
        Rollback does NOT delete intermediate versions. Full history remains intact for audit purposes.<br><br>
        <b>Requirements:</b><br>
        <ul>
        <li>Document must be versioned</li>
        <li>Target version must exist (less than current version count)</li>
        <li>Note explaining rollback reason is required</li>
        </ul>
        <b>Use Cases:</b><br>
        <ul>
        <li>Reverting accidental changes</li>
        <li>Restoring to known-good state</li>
        <li>Undoing problematic updates</li>
        </ul>


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param version: Version number to rollback to (0-indexed)
        :param note: Reason for rollback (required for audit trail)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RollBackToPreviousVersionRequest(
            document_id=document_id,
            body=models.RollBackToPreviousVersionRequestBody(
                version=version,
                note=note,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/document/{documentId}/rollBack",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.RollBackToPreviousVersionRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="rollBackToPreviousVersion",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.RollBackToPreviousVersionResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def rollback_async(
        self,
        *,
        document_id: str,
        version: str,
        note: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.RollBackToPreviousVersionResponse:
        r"""Rollback to previous version

        Restore a versioned document to a previous version. Creates a new version entry with the rolled-back content.<br><br>
        <b>Overview:</b><br>
        This endpoint allows reverting a document to any previous version while maintaining the complete version history. The rollback itself creates a new version entry.<br><br>
        <b>Rollback Flow:</b><br>
        <ol>
        <li>Specify target version number to rollback to</li>
        <li>System retrieves content from that version</li>
        <li>Creates new version entry with restored content</li>
        <li>Adds rollback note to version history</li>
        <li>Document now shows restored content</li>
        </ol>
        <b>Version History Preserved:</b><br>
        Rollback does NOT delete intermediate versions. Full history remains intact for audit purposes.<br><br>
        <b>Requirements:</b><br>
        <ul>
        <li>Document must be versioned</li>
        <li>Target version must exist (less than current version count)</li>
        <li>Note explaining rollback reason is required</li>
        </ul>
        <b>Use Cases:</b><br>
        <ul>
        <li>Reverting accidental changes</li>
        <li>Restoring to known-good state</li>
        <li>Undoing problematic updates</li>
        </ul>


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param version: Version number to rollback to (0-indexed)
        :param note: Reason for rollback (required for audit trail)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.RollBackToPreviousVersionRequest(
            document_id=document_id,
            body=models.RollBackToPreviousVersionRequestBody(
                version=version,
                note=note,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/document/{documentId}/rollBack",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.RollBackToPreviousVersionRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="rollBackToPreviousVersion",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.RollBackToPreviousVersionResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def check_document_modified(
        self,
        *,
        document_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CheckDocumentModifiedResponse:
        r"""Check if document is modified

        Check if the current document content differs from the last saved version. Useful for detecting unsaved changes.<br><br>
        <b>Overview:</b><br>
        Compares the current document buffer with the most recent version in history to detect modifications. Returns true if content has changed since last version.<br><br>
        <b>Use Cases:</b><br>
        <ul>
        <li>Prompting user to save before closing</li>
        <li>Auto-save decision making</li>
        <li>Version creation validation</li>
        <li>Change detection in workflows</li>
        </ul>
        <b>Comparison Method:</b><br>
        Performs binary comparison of file buffers. Identical content returns false even if edited and reverted.<br><br>
        <b>Requirements:</b><br>
        Document must be versioned to have comparison baseline.


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CheckDocumentModifiedRequest(
            document_id=document_id,
        )

        req = self._build_request(
            method="GET",
            path="/document/{documentId}/isModified",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="checkDocumentModified",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.CheckDocumentModifiedResponse, http_res
            )
        if utils.match_response(http_res, ["401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def check_document_modified_async(
        self,
        *,
        document_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CheckDocumentModifiedResponse:
        r"""Check if document is modified

        Check if the current document content differs from the last saved version. Useful for detecting unsaved changes.<br><br>
        <b>Overview:</b><br>
        Compares the current document buffer with the most recent version in history to detect modifications. Returns true if content has changed since last version.<br><br>
        <b>Use Cases:</b><br>
        <ul>
        <li>Prompting user to save before closing</li>
        <li>Auto-save decision making</li>
        <li>Version creation validation</li>
        <li>Change detection in workflows</li>
        </ul>
        <b>Comparison Method:</b><br>
        Performs binary comparison of file buffers. Identical content returns false even if edited and reverted.<br><br>
        <b>Requirements:</b><br>
        Document must be versioned to have comparison baseline.


        :param document_id: Document ID (24-character MongoDB ObjectId)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CheckDocumentModifiedRequest(
            document_id=document_id,
        )

        req = self._build_request_async(
            method="GET",
            path="/document/{documentId}/isModified",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="checkDocumentModified",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.CheckDocumentModifiedResponse, http_res
            )
        if utils.match_response(http_res, ["401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
