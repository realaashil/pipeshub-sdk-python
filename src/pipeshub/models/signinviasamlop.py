"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pipeshub.types import BaseModel, UNSET_SENTINEL
from pipeshub.utils import FieldMetadata, QueryParamMetadata
import pydantic
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SignInViaSAMLRequestTypedDict(TypedDict):
    email: str
    r"""User's email address"""
    session_token: NotRequired[str]
    r"""Session token from `/userAccount/initAuth`. Optional but recommended
    for maintaining authentication state across the SAML flow.

    """


class SignInViaSAMLRequest(BaseModel):
    email: Annotated[
        str, FieldMetadata(query=QueryParamMetadata(style="form", explode=True))
    ]
    r"""User's email address"""

    session_token: Annotated[
        Optional[str],
        pydantic.Field(alias="sessionToken"),
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Session token from `/userAccount/initAuth`. Optional but recommended
    for maintaining authentication state across the SAML flow.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["sessionToken"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SignInViaSAMLResponseTypedDict(TypedDict):
    headers: Dict[str, List[str]]


class SignInViaSAMLResponse(BaseModel):
    headers: Dict[str, List[str]]
