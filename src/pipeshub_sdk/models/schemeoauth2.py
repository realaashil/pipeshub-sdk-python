"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pipeshub_sdk.types import BaseModel
from pipeshub_sdk.utils import FieldMetadata, SecurityMetadata
from typing_extensions import Annotated, TypedDict


class SchemeOauth2TypedDict(TypedDict):
    r"""OAuth 2.0 authentication with fine-grained scopes.
    Supports authorization_code (with PKCE) and client_credentials flows.
    OAuth tokens are Bearer JWTs — use the same Authorization header as regular tokens.

    """

    client_id: str
    client_secret: str
    token_url: str


class SchemeOauth2(BaseModel):
    r"""OAuth 2.0 authentication with fine-grained scopes.
    Supports authorization_code (with PKCE) and client_credentials flows.
    OAuth tokens are Bearer JWTs — use the same Authorization header as regular tokens.

    """

    client_id: Annotated[
        str, FieldMetadata(security=SecurityMetadata(field_name="clientID"))
    ]

    client_secret: Annotated[
        str, FieldMetadata(security=SecurityMetadata(field_name="clientSecret"))
    ]

    token_url: str = "/api/v1/oauth2/token"
